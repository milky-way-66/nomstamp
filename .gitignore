# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode — the project is generated from project.yml, never committed
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/

# macOS
.DS_Store

# Claude Code session scratch
.claude/worktrees/

# Archives, exported .ipa and the generated export options
build/

# Real credentials for scripts/deploy.sh — .env.example is the committed template
.env
